# 기획 질문

기획서가 모호하거나 빈칸이 있을 때 적는다. 추측으로 채우지 않는다. 답은 기획이 Drive의 기획서에 반영한다.

| 날짜 | 질문자 | 기획서 항목 | 질문 | 상태 |
| --- | --- | --- | --- | --- |
| 2026-09-05 | roy0206 | 19. 진행 및 기록 / 35. 기록과 구간 | 일반 구간의 종료 조건은 무엇인가(거리, 시간, 장애물 수). 거리 단위는 무엇이며 구간별 길이(sectionLengths)의 기준값은 얼마인가. 3판 9절에서 시간으로 확정: 25/30/35/15초. 코어루프는 `SectionDurations`로 반영. ScrollSpeed 4 unit/s는 여전히 임시 | 해결(3판) |
| 2026-09-05 | roy0206 | 11. 기본 장애물 / 17. 게임 난이도 | 바위·상어·고래의 접근 속도 기준값과 구간별 증가 폭(배율 또는 가산)은 얼마인가. 장애물 속도가 배경 스크롤 속도와 같은지, 장애물마다 별도 속도를 더하는지도 확정이 필요하다 | **해결(2026-09-06 기획자 답변 4, 반영 완료)**: 구간 속도 배율은 쓰지 않는다(네 구간 전부 1.0). 난이도는 패턴 간격과 티어 비중으로만 만든다. 장애물별 배율(물고기 1.25, 통나무 0.8)은 유지 |
| 2026-09-05 | roy0206 | 11. 기본 장애물 / 18. 충돌 | 충돌 범위(collisionScale)의 기준은 스프라이트 크기 대비 몇 %인가. 상어·고래의 몸길이(가로 길이)와 두 레인을 막는 시간의 목표값은 얼마인가 | 3판에서 삭제됨 (10절: 상어·고래 제외, 돌/물고기/통나무 1레인만. 통나무 길이·물고기 속도 기준값은 여전히 미확정) |
| 2026-09-05 | roy0206 | 8. 점프 | 점프 중(물 밖)에도 부딪히는 장애물이 존재하는가(낚싯바늘, 새 등). 없다면 점프 중 충돌 판정을 완전히 끄는 현재 구현을 유지한다 | 대기 |
| 2026-09-05 | roy0206 | 22·24·26. 보스 / 31. 게임오버 | 보스별 공격 패턴의 구체 내용, 보스 지속 시간(bossDurations) 기준값, 보스에서의 실패 규칙(한 번 충돌 실패 유지 여부)은 무엇인가 | 3판에서 확정(12·13·18절: 30초, 예고 0.8/공격 0.6/휴식 0.6, 한 번 맞으면 해당 보스 처음부터) |
| 2026-09-05 | roy0206 | 8. 점프 / 5. 게임 화면 | 점프 최고 높이(상단 레인 기준 몇 px)와 수면의 y 위치는 얼마인가. 현재 임시값 JumpHeight 1.6 unit(160px), WaterSurfaceY 2.0 unit(200px) | 대기 |
| 2026-09-05 | roy0206 | 13. 일반 장애물 패턴 / 17. 게임 난이도 | 패턴 간격의 기준값은 얼마인가. 스포너는 "이전 패턴의 마지막 장애물이 플레이어를 지난 뒤 다음 패턴의 첫 장애물이 도착하기까지의 여유(초)"로 정의했고 현재값은 4번 기획서 06절 추천 후보 범위 안의 구간 1 2.5→2.0, 2 2.0→1.6, 3 1.7→1.3, 4 2.5→3.0초(70%부터 중단)다. 후보가 최종 확정되면 `DifficultyCurve` 값만 바꾼다 | 대기 (4번 06절 후보 반영) |
| 2026-09-05 | roy0206 | 4번 05절 미확정 / 16절 4항 / 17절 B | 일반 구간에서 점프 필수 패턴(세 레인 동시 봉쇄)을 사용하는가. 스포너는 기능을 유지하고 구간별 `jumpRequiredRatio`(DifficultyCurve)로 켜고 끈다. **기본값은 켬**(구간 1 10→20%, 2 25%, 3 30→35%, 4 10→0%)이며 0으로 두면 점프 필수 패턴이 전혀 나오지 않는다(테스트로 보장) | 해결(v4, 아래 2026-09-06 행: 네 구간 전부 0) |
| 2026-09-05 | roy0206 | 4번 05절 미확정 | 세 장애물의 이동속도 차이(현재 돌 1.0·물고기 1.8·통나무 1.0배), 통나무 점유 시간(현재 몸길이 3.0 unit ≈ 0.83초), 장애물별 등장 확률(현재 패턴 티어 가중치로만 조절), 조합 패턴(현재 28개), 등장 레인 제한(현재 전부 상/중/하 허용)은 전부 임시값이다. 확정되면 `ObstacleDefinition`·`SpawnPattern` 에셋만 바꾼다 | 해결(v4, 아래 2026-09-06 행: 속도·길이 확정. 등장 확률·레인 제한은 대기) |
| 2026-09-05 | roy0206 | 15. 점프 필수 패턴 안전 규칙 | MIN_JUMP_PATTERN_GAP(약 2초)의 정확한 정의는 무엇인가. 스포너는 "이전 점프 필수 패턴의 마지막 장애물이 플레이어를 지난 시각부터 다음 점프 필수 패턴의 첫 장애물이 도착하는 시각까지 2초"로 두고, 점프 필수 직후 패턴 1개는 반드시 일반 패턴으로 했다. 사이에 일반 패턴이 꼭 있어야 하는지, 2초가 스폰 간격인지 도착 간격인지 확인이 필요하다 | 대기 |
| 2026-09-05 | roy0206 | 16. 일반/점프 필수 비율 | 점프 필수 약 25%는 패턴 개수 기준인가 시간 기준인가. 구간별로 다르게 둬도 되는가. 현재는 패턴 개수 기준이고 구간 1 10→20%, 2 25%, 3 30→35%(3판 11절 "점프 활용 증가"), 4 10→0%(70% 지점부터 장애물 없음)다 | 대기 |
| 2026-09-05 | roy0206 | 17. 게임 난이도 | 접근 속도 증가가 배경 스크롤까지 함께 빨라지는 것인가(연어가 빨라 보임), 장애물만 빨라지는 것인가(바위가 물살을 거슬러 다가오는 것처럼 보임). 구간 배율 자체가 **2026-09-06 기획자 답변 4로 사라졌다**(전부 1.0). 스크롤도 물살 표현도 그대로다 | 해결(반영 완료) |
| 2026-09-05 | roy0206 | 15. 안전 규칙 4·6 | "확실한 해결 방법"의 최소 기준은 무엇인가. 스포너는 (1) 장애물이 화면에 다 나타난 뒤 아무 입력 없이 버틸 수 있는 반응 여유 최소 0.4~0.6초, (2) 입력 타이밍 허용 오차 ±0.1초로 두었다. **2026-09-06 기획자 답변 5로 반응 여유 목표가 확정**되어 `DifficultyCurve.minReactionMargin`을 구간 1 1.3 / 2 1.0 / 3 0.8 / 4 1.3초로 넣었다(구간 4는 개발 기본값). 허용 오차 0.1초는 그대로 | 해결(반영 완료) |
| 2026-09-05 | roy0206 | 2판 30. 인게임 UI | 좌측 상단에 "현재 진행 거리 또는 현재 구간" 중 무엇을 기본으로 보일지. 3판 19.3절 기준으로 "구간 n · 남은 s초"와 작은 진행 바로 구현 | 해결(3판, 선택 UI) |
| 2026-09-05 | roy0206 | 2판 17. 체크포인트 | 구간 시작부터 재시작할 때 거리 표시·기록의 기준. 3판에서 구간이 시간 기준이 되어 HUD는 남은 초를 보이므로 거리 표시 문제는 사라짐 | 해결(3판) |
| 2026-09-05 | roy0206 | 2판 24. 보스 3 | 30초 뒤 "상단 이동 안전 구간"의 길이와 마지막 점프 실패 시 처리(재도전 지점이 보스 시작인지 마지막 점프인지) | 대기 |
| 2026-09-05 | roy0206 | 2판 28. 메타 기록 | 2판 28절의 최고 거리·최고 구간·최근 기록은 무한 러너 잔재로 판단해 클리어 횟수만 저장(roy0206 결정). 기획이 다른 기록을 원하면 알려 달라 | 결정됨 |
| 2026-09-05 | roy0206 | 2판 18·20. 보스 공통 / 낚싯줄 | 예고 0.8·공격 0.7·회복 0.5초는 세 보스 공통값인가, 보스마다 다르게 둘 수 있는가. 낚싯바늘이 레인에 머무는 시간(공격 0.7초 중 하강에 40%, 나머지가 위험 구간)과 바늘의 x 위치(플레이어 x와 동일로 둠)를 확인해 달라 | 3판에서 확정(공통 타이밍 동일, 3레인 전체 예고 1.0초 → v4 1.5초, 일반 1.2초). 바늘 x는 **v4 결정으로 해소**: 수직 낙하가 아니라 오른쪽 화면 밖 → 왼쪽으로 훑고 지나가는 트롤링 형태이며 공격 0.6초의 중간에 플레이어 x를 지난다. 판정은 바늘이 아니라 레인 띠(`LaneHazard`)가 한다 |
| 2026-09-05 | roy0206 | 2판 22. 바다코끼리 | 돌진이 플레이어를 지나쳐 화면 왼쪽으로 빠졌다가 오른쪽 대기 위치로 복귀하는 형태로 두었다(복귀는 회복 0.5초, 복귀 중 판정 없음). 대기 위치 x=3, 돌진 끝 x=-7. 2레인 돌진의 몸 높이(2.0 unit)와 1레인(0.9 unit) 구분이 이 정도면 충분한가 | 대기 |
| 2026-09-05 | roy0206 | 2판 24. 폭포 | 급류·돌이 오른쪽(x=7.5)에서 왼쪽(x=-8)까지 공격 0.7초에 지나가게 했다(약 22 unit/s). 3레인 전체 패턴은 플레이어가 공중이거나 점프 쿨타임이 예고 시간(0.8초)보다 길게 남았으면 내지 않는다. 30초 뒤 안전 구간 2초 후 점프 가능해지면 폭포가 깜박여 점프를 유도하고, 플레이어가 점프하지 않으면 무한 대기한다. 시간 제한이나 자동 점프가 필요한가 | 3판 16절로 일부 확정(3레인 전체는 예고 1.0초, 30초 뒤 점프 쿨타임 보정·1번 레인 이동 시간 제공). 점프 안 할 때의 처리는 대기 |
| 2026-09-05 | roy0206 | 2판 18. 보스 공통 / 30. 인게임 UI | 보스 등장(1초)·퇴장(0.8초) 연출 시간이 30초에 포함되는가. 현재는 30초 타이머가 등장 연출 뒤에 시작하고 퇴장은 별도다. 타이머 표시는 정수 초 내림(`BossTimerView`)이다 | 3판 19.2로 UI 확정(상단 중앙 보스 이름 + 정수 초). 등장/퇴장 연출이 30초에 포함되는지는 대기 |
| 2026-09-05 | roy0206 | 4번 07절 히트박스 예고 / 11절 공격 히트박스 | "히트박스 표시"가 (a) 위험 레인을 화면 폭으로 덮는 경고 띠인지, (b) 실제 공격 판정 콜라이더 모양(바늘·몸통·급류·돌)인지, (c) 둘 다인지. 11절 "위험 레인을 명확하게 덮음"은 (a), "실제 공격 판정과 정확히 일치"는 (b)로 읽힌다. 현재는 둘 다 표시한다: 레인 띠(`LaneTelegraph`, 알파 0.3~0.4 펄스, 공격 0.2초 전 0.45)와 공격체 콜라이더 프레임(`HitboxView`, 알파 `bossHitboxAlpha` 0.35, 예고 시작~공격 끝). 하나만 원하면 알려 달라 | 해결(v4). v4 10·14·15절 "위험한 레인 전체를 덮음 + 실제 공격 판정과 반드시 일치"로 **(a) 레인 띠만** 남기고 판정도 띠와 같게 했다. 판정은 새 `LaneHazard`(레인마다 화면 폭 `bossLaneBandWidth` × `bossLaneBandHeight` 트리거)가 하고 공격체(바늘·몸통·급류·돌) 콜라이더는 판정에서 빠졌다. `HitboxView`·`HitboxViewModule`·`HazardHitbox`는 삭제. `bossHitboxAlpha`·`HitboxFrame.png`는 통합 패스에서 삭제했다 |
| 2026-09-05 | roy0206 | 4번 17절 G 히트박스 / 05절 플레이어 폭 | 연어 아트(527×280px, PPU 100, 스케일 0.29)에 알파 박스 × 0.87 히트박스를 씌우면 약 1.33×0.71 unit이 된다. 스포너 시뮬레이션의 `playerHalfWidth`는 0.35(폭 0.7) 기준이라 실제 판정이 시뮬레이션보다 가로로 0.63 unit 넓다(`safetyPadding` 0.1초 = 0.4 unit 안쪽). **2026-09-06 기획자 답변 1로 해결**: 가로를 몸통의 60%(월드 0.92)로 좁혀 `playerHalfWidth`를 0.46으로 맞췄다. 남은 어긋남은 0.004 unit이다 | 해결(반영 완료) |
| 2026-09-05 | roy0206 (Cutscene) | v4 18. 스토리·컷신 / 25. 미확정 | 아이의 이름·나이·「곧 떠난다」의 이유가 비어 있다. 지금은 화자를 `아이`, 엔딩에서 `어른이 된 아이`로 두고 이름을 쓰지 않았다. 이름을 정하면 CSV의 `speaker` 열만 바꾸면 된다. 또 연어의 이름은 이전 판의 `연이`를 그대로 썼는데 v4에는 이름이 없다. 유지할지 확인해 달라 | 대기 |
| 2026-09-05 | roy0206 (Cutscene) | v4 18. 엔딩 | 엔딩에서 연어와 어른이 된 아이가 서로를 알아보는가. 지금은 어른이 된 아이가 「정말로 돌아왔구나.」 한 줄을 말해 알아본 것으로 연출했다. 서로 모른 채 같은 장소에 있는 편이 주제에 맞다면 그 줄을 내레이션으로 바꾼다 | 대기 |
| 2026-09-05 | roy0206 (Cutscene) | v4 18. 인트로 | 인트로가 9줄(내레이션 2 + 아이 4 + 연어 3)이다. 중간 컷신은 5문장 이하 원칙이 있지만 인트로·엔딩에는 길이 원칙이 없다. 타이틀 직후 대기 시간이 길어지므로 목표 길이를 정해 주면 줄이겠다 | 대기 |
| 2026-09-06 | roy0206 (Spawner) | v4 05. 일반장애물 / 통나무 아트 | 통나무 아트의 비율은 1.84:1(알파 박스 531×288 px)이다. 3판 몸길이 3.0 unit으로는 레인 높이 1.0 안에서 이 비율을 맞출 수 없어 9-슬라이스(`drawMode = Sliced`, 좌 130 / 우 110 px border)로 마디만 늘리는 방법을 검토했다 | 해결(v4): 통나무 몸길이가 1.8로 확정되어 균등 스케일만으로 1.80 × 0.976 unit이 된다(레인 표시 높이 1.0 안). 9-슬라이스는 쓰지 않고 돌과 같은 경로로 처리한다 |
| 2026-09-06 | roy0206 (Spawner) | v4 17절 G 히트박스 / 05. 일반장애물 | 돌은 아트가 납작해서(알파 박스 560×252 px) 몸길이 1.0에 균등 스케일을 맞추면 화면 높이가 0.45 unit이다. 그런데 `BoxCollider2D` 세로는 0.80 unit(레인 폭 1.1 − 0.3, 이웃 레인 오염 방지값이자 밸런스 시뮬레이터가 검증한 값)이라 **보이는 돌보다 위아래로 0.175씩 넓다**. 가로는 0.87로 아트와 맞는다. (a) 지금처럼 세로를 레인 기준으로 두고 가로 판정에 맡길지, (b) 세로도 아트에 맞춰 0.45 근처로 줄일지(레인 경계에 여유가 생겨 스치는 판정이 사라진다), (c) 돌 아트를 더 높게 그릴지 정해 달라 | **해결(2026-09-06 기획자 답변 1, (b) 채택 · 반영 완료)**: 세로 = 아트 높이 × 0.87(돌·물고기 0.39, 통나무 0.85). 연어 히트박스도 가로 60%로 좁히고 `playerHalfWidth`를 0.46으로 맞췄다 |
| 2026-09-06 | roy0206 (Spawner) | v4 05. 일반장애물 / 16절 4항 | 일반 구간의 점프 필수 패턴(세 레인 동시 봉쇄)은 v4에서 **쓰지 않기로 확정**되었다(점프 강제는 보스 3만). 네 구간 모두 `jumpRequiredRatio = 0`이고 `Jump_*` 패턴 4개는 라이브러리에만 남는다. 되돌리려면 `DifficultyCurve` 값만 올리면 된다 | 해결(v4) |
| 2026-09-06 | roy0206 (Spawner) | v4 05. 일반장애물 | 세 장애물의 속도·길이가 돌 기준 상대값으로 확정되었다(돌 x1.0/1.0, 물고기 x1.25/0.9, 통나무 x0.8/1.8). 통나무 점유 시간은 약 0.71초가 된다. 남은 미확정이던 등장 확률과 등장 레인 제한도 **2026-09-06 기획자 답변 3으로 확정**했다: 돌 5 : 물고기 3 : 통나무 2(개수 기준, `spawnWeight` + `ObstacleMix`), 돌은 맨 아랫줄 전용, 물고기·통나무는 세 줄 | **해결(반영 완료)** |
| 2026-09-06 | roy0206 (Ledge) | 4판 05절 접근시간 / 25절 미확정 | 상류 단차의 정확한 시각. 지금은 구간 1의 12.5초, 구간 2의 15초(각 구간 25·30초의 중간)로 `LedgeData.ledgeTimes`에 넣었다. 앞뒤로 진입 2.925초 + 접근 2초가 필요하고 구간 끝에서 3초 이상 떨어져야 한다. 확정되면 에셋 값만 바꾼다 | 해결(2026-09-06 답변 6). 진행 1에 5초를 추가해 5·12.5초 두 번, 진행 2는 15초. 데이터가 `ledgeTimes`(구간별 하나)에서 `ledgeEntries`(구간 + 시각 쌍의 목록)로 바뀌었다 |
| 2026-09-06 | roy0206 (Ledge) | 4판 05절 첫 단차 실패 / 14절 단차 실패 | 단차에 막혔을 때 "진행이 막힘"의 범위. 지금은 스크롤·구간 시간·거리·장애물 스폰을 전부 멈추고 플레이어 입력만 살려 둔다(HUD의 남은 초도 멈춘다). 구간 시간만 계속 흐르게 하면 막힌 채로 구간이 끝날 수 있어 멈췄다. 즉시 실패로 바꾸려면 `LedgeData.instantFail`만 켜면 된다 | 해결(2026-09-06 답변 6: 「진행·타이머·스폰 전부 정지 후 재시도」 = 지금 구현 그대로. `instantFail`은 false 유지) |
| 2026-09-06 | roy0206 (Ledge) | 4판 05절 카메라 / 06절 단차 카메라 | 단차를 넘은 뒤 "카메라가 올라간다"의 최종 화면. 상류에서도 같은 사이드뷰·3레인이라 넘기 전후 화면이 같아야 한다고 읽고, 배경을 영구히 내리지 않고 단차 조각만 1레인 내려 평소 강바닥과 잇는다(배경은 0.35만 내렸다 복귀). 배경 자체가 영구히 내려간 그림을 원하면 알려 달라 | 해결(2026-09-06 답변 6: (가) 화면 복귀. 지금 구현 그대로 유지) |
| 2026-09-06 | roy0206 (Ledge) | 4판 05절 단차 형태 / 19절 그래픽 | 단차 벽의 윗변을 `laneY[0] + 1레인` = 2.2(수면 2.0보다 조금 위, 물 밖으로 솟은 바위)로 두었다. 1번 레인(1.1)보다 1레인 높고 점프 정점(3.3)보다 낮아 기본 점프로만 넘어간다. 벽이 수면 위로 솟는 그림이 맞는지, 아니면 수면 아래에서 끝나야 하는지 확인이 필요하다 | 해결(2026-09-06 답변 6: 모양은 「작은 폭포」). 바위 계단 벽 대신 낙수 띠 + 거품으로 바꾸고 낙수 띠의 윗변을 **위쪽 강의 수면**(수면 2.0 + 단차 1.1 = 3.1)에 맞췄다. 넘어야 할 턱의 논리적 높이 2.2는 그대로다. 남은 질문: 낙수 띠가 점프 정점 3.3보다 0.2 낮아 연어가 물줄기 꼭대기를 뚫고 지나간다 — 그 그림이 맞는지 |
| 2026-09-06 | roy0206 | 기획자 답변(DesignQuestionsForPlanner 1~8) | **1** 돌 판정 세로를 그림 높이로 줄이고 연어 판정도 몸통 중심으로 좁힘(폭 60%, 개발 기본값). **2** 점프 중 맞는 것 없음. **3** 등장 비율 돌5:물고기3:통나무2, 돌은 맨 아랫줄만, 물고기·통나무는 3줄. **4** 구간 속도 배율 없음(전부 1.0), 난이도는 패턴 밀도로. **5** 회피 여유 1.3/1.0/0.8초(구간 4는 1.3, 개발 기본값). **6** 단차 진행1 5초 추가 + 12.5초, 진행2 15초 유지; 막히면 진행·타이머·스폰 전부 정지 후 재시도; 넘은 뒤 (가) 화면 복귀; 모양은 작은 폭포. **7** 배너는 30초 밖; 마지막 폭포는 닿으면 실패(접근 약 3초, 개발 기본값), 실패 시 마지막 폭포부터 재시도. **8** "어른이 된 아이" 표기, 연어 이름 연이 유지, 엔딩 문장 유지, 인트로 6~7문장·엔딩 5~6문장 각 20초 이내 | 1·3·4·5 **반영 완료**(스포너/플레이어: 히트박스, 레인 제한, 5:3:2, 속도 배율 제거, 반응 여유). 2는 이미 구현대로. 6·7·8 **반영 완료**(2026-09-06, 단차·보스·컷신 담당): **6** `LedgeData.ledgeEntries` = `[(1,5),(1,12.5),(2,15)]`, `LedgeSet.prefab`에 세 번째 조각 추가, 막힘 동작은 지금 그대로(`instantFail=false`), 넘은 뒤 (가) 유지, 모양은 작은 폭포(`Ledge_Cascade.png`·`Ledge_Foam.png`, 낙수 윗변 = 수면 2.0 + 1.1) — `Docs/Ledge.md`. **7** `WaterfallBossData.finalApproachDuration` 3초 접근 후 닿으면 `Hit` → 실패, `finalClearHeight` 2.2 이상으로 넘으면 통과(무한 대기·깜박임 삭제). 실패하면 `PlayFlow.CheckpointTag = FinalWaterfall`로 마지막 폭포부터 재도전 — `Docs/Boss.md`·`Docs/CoreLoop.md` 「체크포인트 표」. **8** 인트로 9→7줄·엔딩 6→5줄(문장은 지우지 않고 합치기만), 각 약 16.4초·17.9초, 화자·「연이」·엔딩 문장 그대로 — `Docs/Cutscene.md` 「길이」 |
