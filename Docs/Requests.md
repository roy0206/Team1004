# 요청

타인 소유 경로의 변경이 필요할 때 여기에 적는다. 자기가 쓴 항목만 고친다.

| 날짜 | 요청자 | 대상 | 이유 | 제안 | 상태 |
| --- | --- | --- | --- | --- | --- |
| 2026-09-05 | roy0206 (Spawner) | `Assets/Scripts/Player/LaneMoveModule.cs`, 점프 모듈 | 스포너가 플레이어 상태를 `SpawnPlayerState`로 받아 남은 이동 시간·공중 시간·쿨타임으로 시뮬레이션한다. 남은 시간을 모르면 `LaneMoveDuration`·`JumpDuration` 전체를 가정해 보수적으로 계산하므로, 실제 값을 노출하면 스폰 결정이 정확해진다 | `LaneMoveModule.MoveRemaining`(초), 점프 모듈에 `AirborneRemaining`·`CooldownRemaining`(초) 읽기 전용 프로퍼티 추가. 사용처는 `Docs/Spawner.md` 「통합 절차」 | 완료(roy0206: `LaneMoveModule.MoveRemaining`, `JumpModule.AirborneRemaining`·`CooldownRemaining`, `LanePlayer`가 위임. `PlayFlow.Update`가 `SpawnPlayerState.FromPlayer`에 넘긴다) |
| 2026-09-05 | roy0206 | `Assets/Scripts/Spawner/ObstacleSpawner.cs` `BeginSection(int)` | 기획서 2판 반영으로 `GameConfigValues.StageLength`가 구간별 `SectionLengths`/`GetSectionLength(section)`로 바뀌었다. `StageLength`는 `[Obsolete]` 별칭(구간 1 길이)으로 남겨 컴파일은 유지된다 | `BeginSection(int)` 오버로드에서 `GameConfig.Current.GetSectionLength(sectionIndex + 1)`을 쓰도록 변경. PlayFlow는 `BeginSection(index, length)`만 호출한다 | 완료(roy0206: `ObstacleSpawner.BeginSection(int)`이 `GetSectionLength(sectionIndex + 1)`을 쓴다. PlayFlow는 `BeginSectionByDuration`) |
| 2026-09-05 | roy0206 | `Assets/Scripts/Boss/LaneTelegraphModule.cs` 56행 | `SpriteRenderer.DOFade`는 `DOTween.Modules`(asmdef 없음, Assembly-CSharp-firstpass) 확장이라 asmdef 기반 `Game.Boss`에서 컴파일되지 않았다(CS1929, 프로젝트 전체 컴파일 실패) | 사소한 수정으로 `DOTween.ToAlpha(() => renderer.color, v => renderer.color = v, maxAlpha, pulseDuration)`로 바꿨다. 코어 DOTween API만 쓰는 방식은 `Docs/Cutscene.md` 「DOTween 참조 방식」 참고 | 완료(고쳤음) |
| 2026-09-05 | roy0206 (Boss) | `Assets/Scripts/Bootstrap/Editor/CoreLoopSetup.cs` Play 씬 생성 | 보스 통합은 `Assets/GameAssets/Boss/BossSet.prefab`(BossDirector + 보스 3개, `Team1004/Generate Boss Assets`로 생성) 인스턴스를 Play 씬 루트에 하나 두면 끝난다. `BossDirector`가 `Start`에서 `PlayFlow.Current.BossHandler`를 채운다 | Play 씬 생성기에서 `BossSet.prefab`이 있으면 `PrefabUtility.InstantiatePrefab`으로 루트에 배치(ScrollRoot 밖). 없으면 경고만. 절차는 `Docs/Boss.md` 「통합 절차」 | 완료(roy0206: `CoreLoopSetup.InstantiateBossSet`, 없으면 `BossAssetSetup.Generate()` 선행 호출) |
| 2026-09-05 | roy0206 | `Assets/Scripts/Boss/Integration/BossDirector.cs` (또는 `TelegraphedAttackPhase`) | 2판 33절 사운드: 보스 공격 예고·공격 효과음은 보스 상태 전이 시점을 아는 보스 쪽에서만 재생할 수 있다. `AudioManifest.json`에 `boss_telegraph`(Hint 1-1), `boss_attack`(Stab) id를 등록해 두었다. `boss_clear`는 PlayFlow가 재생한다 | 예고 시작 시 `AudioManager.Instance.PlayGlobal("boss_telegraph")`, 공격 시작 시 `PlayGlobal("boss_attack")` 호출(`Core.Audio` 참조 추가). `TryGetInstance`로 가드 | 완료(roy0206: `PatternBoss.ShowTelegraph`가 `boss_telegraph`, 세 공격 페이즈의 `OnAttackBegin`이 `PatternBoss.PlayAttackSfx()`로 `boss_attack` 재생. `Game.Boss` → `Core.Audio`) |
| 2026-09-05 | roy0206 (Dialogue) | `Assets/Scripts/Bootstrap/GameBootstrap.cs`, `Game.Bootstrap.asmdef` | 대사를 `Assets/GameAssets/Design/Dialogue/dialogue.csv`로 분리했다(기획서 3판 20절). `DialogueService`가 로드되지 않으면 컷신 대사가 전부 `[intro.01]` 대체 줄로 나온다 | asmdef 참조에 `Game.Dialogue` 추가, `[SerializeField] private TextAsset dialogueCsv;` 추가, `GameConfig.Load(configAsset);` 다음 줄에 `DialogueService.Load(dialogueCsv);`. Bootstrap 씬에서 `dialogueCsv`에 위 CSV 연결. 코드 조각은 `Docs/Dialogue.md` 「Bootstrap 초기화」 | 완료(roy0206: `GameBootstrap.dialogueCsv` + `DialogueService.Load`, 생성기가 Bootstrap 씬에 CSV 연결) |
| 2026-09-05 | roy0206 (Boss) | `Assets/Scripts/Player/JumpModule.cs`, `LanePlayer` | 기획서 3판 16절: 폭포 보스 30초 종료 시 점프 쿨타임을 0으로 보정해 마지막 점프가 막히지 않게 한다. `JumpModule.CooldownRemaining`은 private set이라 보스가 보정할 수 없어 지금은 쿨타임이 끝날 때까지 안전 구간을 연장하는 방식으로 대체했다 | `JumpModule.ResetCooldown()`(CooldownRemaining=0)과 `LanePlayer.ResetJumpCooldown()` 위임 추가. 생기면 `WaterfallBreakthroughState.OnEnter`에서 호출한다(`Docs/Boss.md` 「보스 3」) | 처리 완료(roy0206: `JumpModule.ResetCooldown()`, `LanePlayer.ResetJumpCooldown()` 추가, `WaterfallBreakthroughState.OnEnter` 첫 줄에서 호출) |
| 2026-09-05 | roy0206 (Cutscene) | `Assets/Scripts/Dialogue/Editor/DialogueCsvTools.cs` `CollectReferences()` | 컷신을 `CutsceneAsset`(ScriptableObject 스텝 목록)에서 컷신별 C# 클래스로 교체해 `CutsceneAsset` 타입이 사라졌다. `AssetDatabase.FindAssets("t:CutsceneAsset")`가 컴파일되지 않는다 | 사소한 참조 수정으로 `CutsceneCatalog.CreateAll(...)`이 만든 각 `CutsceneBase.LineIds`를 모으도록 바꿨다. 공개 시그니처·파일 위치는 그대로이고 Dialogue 폴더는 이 한 파일만 손댔다. 설명은 `Docs/Dialogue.md` 「검증 도구」 | 완료(고쳤음) |
| 2026-09-05 | roy0206 (Environment) | `Assets/Scripts/Bootstrap/Editor/CoreLoopSetup.cs`, `Assets/Scripts/Play/PlayFlow.cs`, `Game.Play.asmdef`, `Game.Bootstrap.Editor.asmdef` | 환경 모듈(무한 스크롤 배경 + 수면)을 `Assets/GameAssets/Environment/Environment.prefab` 하나로 만들었다. 씬 배치와 상태 연결은 코어루프 몫이다. `Game.Environment`는 `Game.Play`를 참조하지 않는다 | Play 씬 생성 전에 `Game.Environment.Editor.EnvironmentSetup.Generate()` 호출 → 프리팹을 **Play 씬 루트에**(ScrollRoot 밖) 배치. `Game.Play`에 `Game.Environment` 참조와 `[SerializeField] EnvironmentThing environment` 추가 후 `environment.SetScrolling(state == PlayState.Running \|\| state == PlayState.Boss)`, 구간 4 진입 시 `SetHomeland(true)`, `Jumped`/`Landed`에서 `Splash(playerX, -0.35f/0.5f)`. `WaterBody`는 붙이지 않는다(Kinematic이라 동작 안 함). 절차 전문은 `Docs/Environment.md` 「통합 절차」 | 완료(roy0206: 생성기가 `EnvironmentSetup.Generate(false)` 후 프리팹을 루트에 배치, `PlayFlow.environment` 연결, Running·Boss에서 스크롤, 구간 4 `SetHomeland`. 점프·착수 첨벙은 `Splash` 대신 `Game.Water.WaterInteractorModule`(플레이어 모듈이 수면에 속도를 전달)로 바꿨다. `Splash`는 컷신용 API로 남김. `Docs/Environment.md` 「수면 상호작용」) |
| 2026-09-05 | roy0206 (Animation) | `Assets/GameAssets/Art/물고기 애니메이팅/기본·올라가기·내려가기·점프/*.png` 11장 (아트 미러, Drive 정본) | 아트: 캐릭터 크기에 맞춘 캔버스로 요청. 지금은 1920×1080 캔버스에 연어가 527×280 px(전체의 7%)만 차지해, 무압축 RGBA로 한 장 7.9 MiB·**11장 약 87 MiB**가 그대로 상주하고 아틀라스에도 들어가지 않는다 | 연어 외곽에 맞춘 캔버스(예: 560×300)로 11장 전부 다시 내보내 주면 약 6.6 MiB가 된다. **11장 모두 같은 캔버스·같은 연어 위치**여야 클립 사이에 그림이 튀지 않는다(pivot이 11장 합집합 알파 박스 중심 하나뿐이다). pivot은 `Team1004/Generate Animation Assets`가 다시 계산하므로 값만 갱신하면 된다. 파일명 인덱스 앞 공백(`물고기 올라가기 -1.png`)은 생성기가 관대하게 읽으므로 급하지 않지만, 통일해 주면 좋다. 설명은 `Docs/Animation.md` 「메모리 비용」 | 대기 |
| 2026-09-05 | roy0206 (Animation) | `Assets/Scripts/Player/LanePlayer.cs`, `Game.Player.asmdef` | 스프라이트 플립북 모듈 `Game.Animation`을 만들었지만 플레이어 통합은 다른 작업자가 Player 폴더를 쓰고 있어 넘긴다. 붙이지 않으면 점프·수영 그림이 바뀌지 않는다 | asmdef에 `Game.Animation` 참조 추가, `LanePlayer`에 `[SerializeField] private CustomAnimation swimClip/jumpClip/idleClip`와 `AddModule(new SpriteAnimatorModule(GetComponent<SpriteRenderer>()))`, `OnModuleJumped`에서 `Play(jumpClip, GameConfig.Current.JumpDuration)`, `OnModuleLanded`에서 `Play(swimClip)`. 클립은 `Assets/GameAssets/Design/Animations/`. 코드 조각과 스케일 주의점은 `Docs/Animation.md` 「통합 절차」 | 완료(roy0206: `LanePlayer`에 클립 4개(`swimClip`/`laneUpClip`/`laneDownClip`/`jumpClip`)·`SpriteAnimatorModule`, 생성기가 클립과 `물고기 기본-1` 스프라이트·`playerScale`(0.29)·11장 합집합 알파 박스 히트박스를 연결. `idleClip`은 없앴다(수영 0프레임이 정지 포즈). 애니메이터는 `InputEnabled`를 따라 멈춘다. `Docs/Animation.md` 「통합 절차」) |
| 2026-09-05 | roy0206 (통합) | `Assets/GameAssets/Art/` (아트 요청) | 보스 공격 히트박스 표시(기획 4번 11절: 반투명 빨간 직사각형, 30~40% 투명도)를 실제 `BoxCollider2D` 크기에 맞춰 늘리는 9-slice 스프라이트가 필요하다. 지금은 생성기가 만든 `Assets/GameAssets/Placeholder/HitboxFrame.png`(64×64, 테두리 8px 불투명, 안쪽 25% 알파)를 쓴다 | 9-slice용 히트박스 프레임 1장: 정사각형 작은 캔버스(예: 64×64), 네 변 테두리 두께 동일(예: 8px)로 대칭, 안쪽 채움은 알파를 굽지 않고 흰색(색·알파는 코드가 `bossHitboxAlpha`로 입힌다). 임포터 border를 테두리 두께로 잡으면 `SpriteDrawMode.Sliced`로 어떤 크기든 늘어난다 | 철회(roy0206 통합 2026-09-06: v4로 공격체 히트박스 프레임(`HitboxView`)이 없어지고 레인 띠만 남아 이 스프라이트가 더 필요 없다. `HitboxFrame.png`·`bossHitboxAlpha`도 삭제했다. 아트 작업 불필요) |
| 2026-09-05 | roy0206 (Cutscene) | `Assets/Scripts/Bootstrap/Editor/CoreLoopSetup.cs` Play 씬 생성 | 기획 v4 18절 스토리(연어의 약속)에 인간 아이가 인트로·엔딩에 등장한다. 컷신 배우 `child`가 씬에 없으면 인트로·엔딩에서 아이가 보이지 않고 `[Cutscene] Cutscene actor is not found: 'child'` 경고만 난다 | `CreateCutscenePlayer(...)` 직후 `Game.Cutscene.Editor.CutsceneActorSetup.EnsureActors(cutscenePlayer)`(또는 씬을 넘기는 `EnsureActors(scene)`) 한 줄 호출. 생성기가 `CutsceneChild`(Placeholder/Square.png, 렌더러 꺼짐, id `child`)를 만들어 `CutscenePlayer.actors`에 덧붙인다. 멱등이라 여러 번 불러도 배우가 늘지 않는다. `Game.Bootstrap.Editor.asmdef`는 이미 `Game.Cutscene.Editor`를 참조하지 않으므로 참조 추가 필요. 설명은 `Docs/Cutscene.md` 「배우 id 규약」 | 완료(roy0206 통합: `CoreLoopSetup.CreatePlayScene`이 `CreateCutscenePlayer` 직후 `CutsceneActorSetup.EnsureActors(cutscenePlayer)`를 부른다. asmdef는 이미 `Game.Cutscene.Editor`를 참조하고 있었다. Play 씬에 `CutsceneChild`(id `child`) 배우가 들어간다) |
| 2026-09-05 | roy0206 (Cutscene) | `Assets/Scripts/Bootstrap/GameInput.inputactions` `Cutscene/Advance` | 기획 v4 21절: 컷신 넘기기는 Enter와 마우스 클릭이고 Space는 쓰지 않는다(점프도 Space가 아니다) | 바인딩을 `<Keyboard>/space` 하나에서 `<Keyboard>/enter`, `<Keyboard>/numpadEnter`, `<Mouse>/leftButton` 셋으로 바꿨다. `Cutscene` 맵만 손댔고 `Player`·`UI` 맵은 그대로다. 액션 이름·GUID는 유지해 코드 변경이 없다(`CutscenePlaybackModule`은 액션 이름으로만 듣는다). 대사 힌트 문구는 `Enter / 클릭 ▶` | 완료(고쳤음) |
| 2026-09-06 | roy0206 (Spawner) | `Assets/GameAssets/Art/오브젝트/돌1.png`, `돌 2.png`, `통나무.png`, `장애물 물고기/장애물 물고기1.png`, `장애물 물고기2.png` (아트 미러, Drive 정본) | 아트: 오브젝트 크기에 맞춘 캔버스로 요청. 연어 11장과 같은 문제다. 다섯 장 모두 1920×1080 캔버스인데 실제 그림은 돌 560×252 px, 통나무 531×288 px, 장애물 물고기 410×205 px(캔버스의 7% 안팎)뿐이다. 무압축 RGBA로 한 장 7.9 MiB라 다섯 장 약 40 MiB가 그대로 상주하고 아틀라스에도 들어가지 않는다 | 오브젝트 외곽에 맞춘 캔버스(돌 예 `576×264`, 통나무 예 `544×304`, 장애물 물고기 예 `432×224`)로 다시 내보내 주면 다섯 장 합쳐 약 2.6 MiB가 된다. 돌·통나무는 **파일마다 pivot이 따로**라 같은 캔버스일 필요가 없지만, **`장애물 물고기` 폴더의 2장은 같은 캔버스·같은 물고기 위치**여야 한다(두 장 합집합 알파 박스 중심 하나를 pivot으로 공유해 2프레임 애니메이션이 튀지 않게 한다). 캔버스가 바뀌어도 `ObjectArtPostprocessor`가 임포트 때 pivot을 다시 잡고 `Team1004 > Generate Spawner Assets`가 프리팹 스케일을 다시 계산한다. **장애물 물고기는 왼쪽을 보고 있어 진행 방향(오른쪽 → 왼쪽)과 맞다. 좌우 반전이 필요 없으니 이대로 두면 된다.** 설명은 `Docs/Spawner.md` 「프리팹 비주얼」, `Docs/Animation.md` 「후처리기 범위」 | 대기 |
| 2026-09-06 | roy0206 (Spawner) | `Assets/GameAssets/Art/오브젝트/돌1.png`, `돌 2.png` (아트 미러, Drive 정본) | 돌 변종 2종을 받아 `ObstacleThing.variants`에 넣고 시드 기반으로 골라 쓰게 했는데, 두 파일의 내용이 **바이트 단위로 같다**(md5 `43bba834…` 동일). 변종 선택은 도는데 화면에서는 차이가 없다 | 두 번째 돌을 다른 그림으로 다시 올려 주면 된다(형태·크기는 자유. 알파 박스가 달라도 프리팹은 기본 스프라이트인 `돌1` 기준으로 스케일을 잡는다). 변종을 쓰지 않기로 하면 `돌 2.png`를 지우고 알려 달라 — 생성기가 있는 파일만 `variants`에 넣는다 | 대기 |
| 2026-09-06 | roy0206 (Ledge) | `Assets/Scripts/Spawner/ObstacleSpawner.cs`, `SpawnModule.cs` | 상류 단차 접근 2초 동안 **새 장애물 스폰만** 멈춰야 한다(기획서 4판 05절). `Stop()`은 `Advance` 전체를 막아 이미 떠 있는 장애물이 그 자리에 얼어붙고 회수(`MoveAndRecycle`)도 멈춘다. `BeginSection*`을 다시 부르면 구간 시각·난이도가 초기화된다 | `SpawnModule`에 `public bool SpawningEnabled { get; set; }`(기본 true)를 두고 `Advance`에서 `MoveAndRecycle(deltaTime)`까지는 그대로 한 뒤 `if (!SpawningEnabled) return;`을 `generator.SpawnStopped` 검사 앞에 넣는다. `ObstacleSpawner.SetSpawningEnabled(bool)`로 위임. 사용처와 호출 줄은 `Docs/Ledge.md` 「통합 절차」 2-(h) | 완료(roy0206 통합: `SpawnModule.SpawningEnabled`(기본 true, `Begin`에서 true로 복귀), `Advance`가 `MoveAndRecycle` 뒤 `SpawningEnabled`가 false면 스폰 판단만 건너뛴다. `ObstacleSpawner.SetSpawningEnabled`/`SpawningEnabled`. `PlayFlow.Update`가 매 프레임 `!ledge.IsSpawnSuspended`를 넘긴다) |
| 2026-09-06 | roy0206 (Ledge) | `Assets/Scripts/Play/PlayFlow.cs`, `Game.Play.asmdef`, `Assets/Scripts/Bootstrap/Editor/CoreLoopSetup.cs`, `Game.Bootstrap.Editor.asmdef` | 상류 단차 모듈(`Game.Ledge`)을 `Assets/GameAssets/Ledge/LedgeSet.prefab` 하나로 만들었다. 씬 배치와 상태 연결은 코어루프 몫이다. `Game.Ledge`는 `Game.Play`를 참조하지 않는다(`BossDirector`/`BossHandler`와 같은 형태) | `Game.Play`에 `Game.Ledge` 참조 + `[SerializeField] LedgeDirector ledgeDirector`·`public ILedgeHandler LedgeHandler`, `Update`에서 `Tick(dt)` 후 `IsHoldingWorld`면 스크롤·거리·구간 시간 정지, `RunSectionAsync`에서 `BeginSection(section, sectionDuration)`·종료 시 `Stop()`, `Fail()`에서 `Stop()`. 코어루프 생성기는 `LedgeSetup.Generate(false)` 후 프리팹을 Play 씬 루트(ScrollRoot 밖)에 두고 `environment`·`player`·`PlayFlow.ledgeDirector`를 연결. 붙일 줄 전문은 `Docs/Ledge.md` 「통합 절차」 | 완료(roy0206 통합: `Docs/Ledge.md` 「통합 절차」 1~4 전부 반영. `PlayFlow`가 `Tick` → `IsHoldingWorld`면 스크롤·환경·거리·구간 시간·스포너 정지, `IsSpawnSuspended` → `SetSpawningEnabled`, 구간 시작 `BeginSection`·종료/실패/디버그 점프 `Stop()`. 생성기가 `LedgeSetup.Generate(overwrite)` 후 `LedgeSet`을 루트에 두고 참조를 채운다. `FullLoopTests`가 단차 통과를 확인한다) |
| 2026-09-06 | roy0206 (Ledge) | `Assets/GameAssets/Art/` (아트 요청) | 상류 단차 지형(기획서 4판 19절: 아래 강 / 1레인 높이의 지형 / 위쪽 강 / 첫 단차 배경 `↑`). 지금은 생성기가 만든 임시 텍스처 `Assets/GameAssets/Ledge/Ledge_Rock.png`·`Ledge_RockEdge.png`·`Ledge_Hint.png`를 쓴다 | 가로로 이음매 없이 반복되는 바위 타일 1장(예: 128×64)과 윗면이 밝은 변형 1장, 그리고 배경에 그려진 것처럼 보이는 `↑` 1장(정사각 캔버스, 알파 포함). `SpriteRenderer.drawMode = Tiled`로 늘리므로 가로 반복이 맞아야 한다. 색·크기는 코드가 정한다 | 갱신(2026-09-06 기획 답변으로 단차 모양이 「작은 폭포」로 바뀌어 계단 벽 바위 타일(`Ledge_RockEdge.png`)은 필요 없어졌다. 아래 「상류 단차 = 작은 폭포」 행으로 대체. 위쪽 강바닥 타일(`Ledge_Rock.png`)과 `↑`(`Ledge_Hint.png`)는 그대로 필요하다) |
| 2026-09-06 | roy0206 (CoreLoop) | `Assets/Scripts/Player/JumpModule.cs`, `LanePlayer` | 기획 v4 23절 디버그 단축키(1~8)로 구간을 건너뛸 때 플레이어 상태를 초기화한다. `ResetHit`·`ResetJumpCooldown`·`SnapToLane`은 있는데 **공중 상태를 취소하는 API가 없다**. 점프 중에 단축키를 누르면 `JumpModule`이 남은 `JumpDuration` 동안 계속 y를 덮어써서 `SnapToLane`이 무시된다 | `JumpModule.CancelJump()`(IsAirborne=false, elapsed=0, y를 상단 레인으로, `Landed` 미발생)와 `LanePlayer.CancelJump()` 위임 추가. 생기면 `PlayFlow.PrepareDebugJump`에서 `ResetHit` 앞에 호출한다. 설명은 `Docs/CoreLoop.md` 「디버그 단축키」 | 완료(roy0206 통합: `JumpModule.CancelJump()`(공중이면 `IsAirborne=false`, `elapsed=0`, y를 점프 시작 레인으로. `Landed` 미발생·쿨타임 미부여), `LanePlayer.CancelJump()`가 위임 후 수면 속도 초기화·수영 클립. `PrepareDebugJump`가 `ResetHit` 앞에서 부른다) |
| 2026-09-06 | roy0206 (Environment) | `Assets/GameAssets/Art/` (아트 요청) | 물속 배경. 기획서 22절 "물속 배경 1~2종 반복". 지금은 물 뒤판(불투명 SpriteShape) 채움이 임시 세로 그라데이션 한 장(`Assets/GameAssets/Placeholder/Env_Underwater.png`, 64×512)뿐이라 물속이 단색으로 보인다 | 물속 채움 텍스처 **2~3종**. 세로로 위가 밝고 아래가 어두운 그라데이션 위에 수초·빛줄기·자갈 같은 무늬를 얹는다. **가로로 이음매 없이 반복**되어야 하고 알파는 1(불투명). 권장 크기 256×1024, 세로가 수심 6.5 unit에 한 번 매핑된다. 배치 방법은 `Docs/Environment.md` 「물 / 뒤판」 | 대기 |
| 2026-09-06 | roy0206 (Environment) | `Assets/GameAssets/Art/` (아트 요청) | 물 앞판(수면 너머 느낌). 같은 수면 스플라인을 덮는 반투명 폴리곤이며 지금은 흰색 4×4 단색(`Env_Overlay.png`)에 색 (0.55, 0.8, 0.95, 0.15)만 곱하고 있다 | 잔물결 텍스처 1장. 옅은 물빛 위에 가로로 흐르는 잔물결·굴절 무늬. **가로·세로 모두 이음매 없이 반복**, 권장 256×256, 알파는 무늬 대비만 담고 전체 농도는 코드가 `overlayAlpha`(0.15)로 정한다. 진하면 장애물 실루엣이 흐려지니 대비를 낮게 | 대기 |
| 2026-09-06 | roy0206 (Environment) | `Assets/GameAssets/Art/` (배경 아트 규칙) | 카메라 ortho 3.6에 수면 y 2.0이라 **물 위로 보이는 띠는 y 2.0~3.6, 화면 세로의 22%뿐**이고 그 위쪽에 HUD가 겹친다. 지금 쓰는 임포트 에셋(`Free 2D Cartoon Parallax Background/!_Moutain`)도 지면 층 나무 우듬지가 화면 위에서 잘린다 | 배경 층 아트를 그릴 때 **화면 상단 20%(y 2.9 이상)에는 중요한 요소를 두지 않는다.** 실루엣의 핵심(산등성이·나무 우듬지·구조물 꼭대기)은 y 2.0~2.9 안에 들어오게 그린다. 층별 스케일·y는 `Docs/Environment.md` 「원근 (층 표)」 | 대기 |
| 2026-09-06 | roy0206 (Ledge) | `Assets/GameAssets/Art/` (아트 요청) | 상류 단차의 모양이 기획 답변(2026-09-06 「단차 모양은 작은 폭포」)으로 바뀌었다. 계단 벽(바위)이 아니라 **위쪽 강물이 단차를 타고 쏟아지는 작은 폭포**여야 한다. 지금은 PIL로 만든 임시 텍스처 `Assets/GameAssets/Placeholder/Ledge_Cascade.png`(64×128, 세로 물줄기)와 `Ledge_Foam.png`(128×64, 거품)를 쓴다 | 두 장이 필요하다. **(1) 낙수 띠**: 권장 64×128(또는 128×256), **세로·가로 모두 이음매 없이 반복**(`drawMode = Tiled`로 폭 1.2 unit × 높이 6.9 unit에 늘린다). 위(수면 위 crest)가 밝고 아래로 갈수록 푸르게, 세로 물줄기 몇 가닥. 알파는 무늬 대비만 담고 전체 농도는 코드가 정한다. **(2) 거품 바닥**: 권장 128×64, **가로 이음매 없이 반복**, 아래쪽이 짙고 위로 갈수록 알파가 빠지는 흰 거품(폭 2.4 unit × 높이 0.9 unit로 늘린다). **낙수 띠의 윗변은 위쪽 강의 수면(수면 y 2.0 + 단차 1.1 = 3.1)에 맞고 아랫변은 강바닥 −3.8이다.** 거품은 아래 강 수면(y 2.0)에 놓인다. 배치·정렬(−5)·크기는 `Docs/Ledge.md` 「단차 모양 (작은 폭포)」 | 대기 |
| 2026-09-06 | roy0206 (Water) | `Assets/GameAssets/Boss/WaterfallBoss.prefab` (`Rapid0/1/2`, `Rock0/1/2`, `Waterfall`) | 수면 상호작용에 **가로 항적(wake)**을 넣어서 수면 아래를 가로로 지나가는 것도 물결을 만든다(`Docs/Environment.md` 「물 튀김 / 수면 상호작용」). `FishingLineBoss`·`WalrusBoss`는 이미 `WaterInteractor`가 있어 자동으로 항적이 생기지만 **폭포 보스에는 `WaterInteractor`가 하나도 없다.** 급류가 훑고 지나가도 수면이 반응하지 않는다. Boss 프리팹은 내 소유가 아니라 직접 넣지 않았다 | 아래 7개 자식 각각에 `WaterInteractor` 컴포넌트 하나씩. **C# 변경도 asmdef 변경도 필요 없다**(YAML만, `Game.Boss`는 `Game.Water`를 참조할 필요가 없다). 블록 형태는 `Assets/GameAssets/Obstacles/Rock.prefab` 맨 끝을 그대로 베끼면 된다. `<새 fileID>`는 그 파일 안에서 겹치지 않는 int64 아무 값, `<대상 GameObject fileID>`는 아래 표 값이고, 만든 컴포넌트를 그 GameObject의 `m_Component:` 목록 끝에 `- component: {fileID: <새 fileID>}`로 추가한다.<br><br>`--- !u!114 &<새 fileID>`<br>`MonoBehaviour:`<br>` m_ObjectHideFlags: 0` / `m_CorrespondingSourceObject: {fileID: 0}` / `m_PrefabInstance: {fileID: 0}` / `m_PrefabAsset: {fileID: 0}`<br>` m_GameObject: {fileID: <대상 GameObject fileID>}`<br>` m_Enabled: 1` / `m_EditorHideFlags: 0`<br>` m_Script: {fileID: 11500000, guid: de691d1c2f2a7b5438de85ee8d6a10e9, type: 3}`<br>` m_Name: ` / `m_EditorClassIdentifier: Game.Water::Game.Water.WaterInteractor`<br>` shape: {fileID: 0}`<br>` size: {x: <아래>, y: <아래>}`<br>` offset: {x: 0, y: 0}`<br>` ignoreColliderShape: 1`<br>` surfaceInfluence: <아래>`<br><br>**`size`는 로컬 단위**다(`lossyScale`이 곱해진다). 이 프리팹은 스프라이트가 전부 0.64 정사각형이라 셋 다 `{x: 0.64, y: 0.64}`이고 스케일이 월드 크기를 만든다.<br><br>`Rapid0` go `8098532496411008287` / `Rapid1` go `4033575569077388081` / `Rapid2` go `1037915567820314550` — `size {x: 0.64, y: 0.64}`(스케일 5×1.40625 → 월드 3.2×0.9), `surfaceInfluence: 1`. 상단 `Rapid0`(y 1.1, 윗변 1.55)만 proximity 0.55로 반응하고 중·하단은 안 난다(정상).<br>`Rock0` go `1253204758042177747` / `Rock1` go `7767277941593006039` / `Rock2` go `6268396957232636079` — `size {x: 0.64, y: 0.64}`(스케일 1.09375 → 월드 0.7×0.7), `surfaceInfluence: 1`. 상단만 proximity 0.45.<br>`Waterfall` go `890718180462527105` — `size {x: 0.64, y: 0.64}`(스케일 2.34375×12.5 → 월드 1.5×8.0), `surfaceInfluence: 1`. 윗변 4.0이 수면 2.0보다 위라 **proximity 1(최대 세기)**이다. 마지막 폭포가 가로로 움직이는 동안만 항적이 난다.<br><br>주의: `Telegraph/Lane0..2`와 `Band0..2`(Hazard)에는 붙이지 않는다. 예고 띠는 연출이고 Band는 판정만 한다. `WaterBody`는 쓰지 말 것 — 가로 속도를 `Rigidbody2D.linearVelocityX`에서 읽으므로 Kinematic이면 항상 0이다. 세기 조정은 `WaterSettings.asset`의 `wakeVelocityTransfer`(0.15) | 완료(roy0206: `WaterfallBoss.prefab` 7개 자식에 `WaterInteractor` YAML 직접 추가. `Rapid0` 6277505320994043527 / `Rapid1` 2356755070914930296 / `Rapid2` 8754860628708313266 / `Rock0` 3739946082907635736 / `Rock1` 8549857388527782082 / `Rock2` 53782189695123478 / `Waterfall` 2439500943135219386, 전부 `size {x: 0.64, y: 0.64}`·`ignoreColliderShape: 1`·`surfaceInfluence: 1`. `Telegraph`·`Band`는 제외) |
| 2026-09-06 | roy0206 (Water) | `Assets/GameAssets/Ledge/LedgeSet.prefab` (`Ledge1/2/3`의 `Cascade`, `Foam`) | 위와 같은 이유다. 상류 단차의 작은 폭포가 왼쪽으로 흘러 지나가는데 수면이 전혀 반응하지 않는다. `Cascade`(폭 1.2 × 높이 6.9, 로컬 y −0.35 → 윗변 3.1 · 아랫변 −3.8)는 수면 2.0을 **세로로 관통**하므로 proximity 1이 나와 프로젝트에서 가장 센 항적이 된다. `Foam`(2.4 × 0.9, y 2.0)도 수면을 물고 있어 proximity 1이다. Ledge 프리팹은 내 소유가 아니라 직접 넣지 않았다 | 위 폭포 보스 행과 **똑같은 YAML 블록**을 6개 자식에 하나씩. asmdef·C# 변경 없음. `size`는 스케일이 전부 1이라 월드 크기와 같다.<br><br>`Cascade` go `564968814803287103`, `2326382574434329732`, `5019708446603798507` — `size: {x: 1.2, y: 6.9}`, `offset: {x: 0, y: 0}`, `ignoreColliderShape: 1`, `surfaceInfluence: 1`.<br>`Foam` go `2319824982420903885`, `3017439198734441732`, `5682151003576889089` — `size: {x: 2.4, y: 0.9}`, `offset: {x: 0, y: 0}`, `ignoreColliderShape: 1`, `surfaceInfluence: 1`.<br><br>`Cascade`만 넣고 `Foam`은 빼도 된다(둘 다 같은 x 근처라 겹친다). **`IsHoldingWorld`로 세계가 멈춘 동안에는 단차가 안 움직이므로 항적도 멈춘다** — 의도한 대로다. `UpperBed`(30 × 2.7)에는 붙이지 않는다. 폭 30이 화면보다 넓어 수면 전체가 한 번에 눌린다. 너무 세면 `WaterInteractor.surfaceInfluence`가 아니라 `WaterSettings.wakeVelocityTransfer`를 내리거나, 이 둘만 약하게 하려면 `Docs/Environment.md` 「조정」대로 개별 값을 손본다 | 완료(roy0206: `LedgeSet.prefab` 6개 자식에 `WaterInteractor` YAML 직접 추가. `Cascade` 5350479191009760343 / 5214855838574162071 / 3746059751322021188 은 `size {x: 1.2, y: 6.9}`, `Foam` 5555087352104814481 / 6695880868053076157 / 8865677710404613873 은 `size {x: 2.4, y: 0.9}`, 전부 `ignoreColliderShape: 1`·`surfaceInfluence: 1`. `Foam`도 넣었고 `UpperBed`는 제외) |
| 2026-09-06 | roy0206 (Spawner) | `Docs/CoreLoop.md` 「Play 씬 오브젝트」 목록의 `Player` 줄(현재 103행) | 2026-09-06 기획자 답변 1로 연어 히트박스가 **알파 박스 × 0.87**에서 **가로 ×0.6 · 세로 ×0.87**로 바뀌었다. 같은 문서의 히트박스 문단(498행)은 내가 고쳤지만, 씬 오브젝트 목록 줄은 코어루프 생성기(`CoreLoopSetup`)가 만드는 내용을 적은 것이라 소유자에게 넘긴다. 애니메이션 담당이 연어 프레임을 11장 → 12장(`충돌` 포즈 추가)으로 늘린 것도 같은 줄에 걸린다 | `BoxCollider2D trigger = 11장 합집합 알파 박스×0.87` → `BoxCollider2D trigger = 12장 합집합 알파 박스(531×280 px) 기준 가로 ×0.6 · 세로 ×0.87 = 월드 0.92×0.71`. `CoreLoopSetup`이 콜라이더를 계산한다면 폭 배율 0.6을 넣어야 다시 생성해도 값이 유지된다(지금은 `Play.unity` YAML을 직접 고쳐 두었다). 근거는 `Docs/Spawner.md` 「플레이어 히트박스」 | 대기 |
| 2026-09-06 | roy0206 (Environment) | `Assets/GameAssets/Art/` (아트 요청) | 강바닥. 맨 아랫줄 장애물(`Rock`)이 「바닥에 놓인 바위」로 읽히려면 바닥 그림이 있어야 하는데 수면 아래가 불투명 물 폴리곤뿐이었다. 지금은 PIL로 만든 임시 모래 텍스처 `Assets/GameAssets/Placeholder/Env_Riverbed.png`(400×500)를 쓴다 | 강바닥 타일 **1~2장**. **캔버스 400×500 px**(PPU 100 → 4.0 × 5.0 unit). `drawMode = Tiled`로 가로 2회 · 세로 1회 반복하므로 **가로로 이음매가 없어야** 하고 세로는 반복하지 않는다. 내용은 강바닥 모래: 따뜻한 베이지에서 아래로 갈수록 어두워지고 잔물결(dune ripple) 선이 얹힌다. **윗선 규칙이 핵심이다** — 위쪽은 투명하고 모래 실루엣의 **평균 윗선이 위에서 20 px(= 0.20 unit)**, 요철은 그 평균선 기준 **±8 px(= ±0.08 unit)를 넘지 않는다**. 이 규칙이 지켜져야 스프라이트 윗변을 -1.45에 두었을 때 모래 윗선이 정확히 y -1.65(맨 아랫줄 레인 바닥 = `Rock` 아랫변)에 오고 돌이 모래 위에 얹혀 보인다. 요철이 크면 돌이 파묻혀 보인다. 알파는 straight, 밉맵 없음, wrap Repeat, FullRect. 같은 PNG를 상류 단차의 위쪽 강바닥(`LedgeSet.prefab` `UpperBed`)도 그대로 쓴다. 배치·정렬(-5)·숫자는 `Docs/Environment.md` 「강바닥」, `Docs/Ledge.md` 「강바닥 맞춤」 | 대기 |
